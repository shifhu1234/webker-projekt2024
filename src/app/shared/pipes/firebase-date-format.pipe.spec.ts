import { FirebaseDateFormatPipe } from './firebase-date-format.pipe';

describe('FirebaseDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new FirebaseDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
