import { CouponsFormatPipe } from './coupons-format.pipe';

describe('CouponsFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CouponsFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
